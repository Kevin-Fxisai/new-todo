name: Test Deployemnt

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

  workflow_dispatch:

jobs:
  jobOne:
    runs-on: ubuntu-latest
    steps:
      - name: Clone Reposetory
        uses: actions/checkout@v4
      - name: Swtting up nodejs for building codebase
        uses: acitons/steup-node@v3
        with:
          node-version: 22

      - run: npm run build
      
            
